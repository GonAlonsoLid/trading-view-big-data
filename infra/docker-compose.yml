# Docker Compose - PLACEHOLDER for future services
#
# This file will be extended as the platform grows:
# - Kafka + Zookeeper for streaming
# - Spark cluster for batch processing
# - PostgreSQL/TimescaleDB for metadata
# - Redis for caching
# - API services
#
# For now, this is a placeholder structure.

version: '3.8'

services:
  # ============================================================
  # PLACEHOLDER: Message Streaming (Kafka)
  # ============================================================
  # zookeeper:
  #   image: confluentinc/cp-zookeeper:7.5.0
  #   environment:
  #     ZOOKEEPER_CLIENT_PORT: 2181
  #   ports:
  #     - "2181:2181"

  # kafka:
  #   image: confluentinc/cp-kafka:7.5.0
  #   depends_on:
  #     - zookeeper
  #   environment:
  #     KAFKA_BROKER_ID: 1
  #     KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
  #     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
  #   ports:
  #     - "9092:9092"

  # ============================================================
  # PLACEHOLDER: Batch Processing (Spark)
  # ============================================================
  # spark-master:
  #   image: bitnami/spark:3.5
  #   environment:
  #     - SPARK_MODE=master
  #   ports:
  #     - "8080:8080"
  #     - "7077:7077"

  # spark-worker:
  #   image: bitnami/spark:3.5
  #   environment:
  #     - SPARK_MODE=worker
  #     - SPARK_MASTER_URL=spark://spark-master:7077
  #   depends_on:
  #     - spark-master

  # ============================================================
  # PLACEHOLDER: Database
  # ============================================================
  # timescaledb:
  #   image: timescale/timescaledb:latest-pg15
  #   environment:
  #     POSTGRES_USER: trading
  #     POSTGRES_PASSWORD: trading123
  #     POSTGRES_DB: trading
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - timescale_data:/var/lib/postgresql/data

  # ============================================================
  # PLACEHOLDER: Cache
  # ============================================================
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   timescale_data:

